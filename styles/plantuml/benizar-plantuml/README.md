# PlantUML overrides: benizar-plantuml

Base preset:

- `styles/plantuml/benizar-plantuml.puml`

Type-specific overrides:

- `styles/plantuml/benizar-plantuml/<diagram-type>.puml`

Consumers should inject the base preset after `@start...` and append the matching type override when available.
