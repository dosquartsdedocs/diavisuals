# Mermaid overrides: benizar-mermaid

Base preset:

- `styles/mermaid/benizar-mermaid.json`

Type-specific overrides:

- `styles/mermaid/benizar-mermaid/<diagram-type>.mmd`

Each file is intended to be prepended to the Mermaid source before rendering. Most files contain a `%%{init: ...}%%` directive.
